{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "comment": "쓰는 것만 준다. opener:default에는 allow-reveal-item-in-dir가, dialog:default에는 allow-save/allow-message가 딸려 오는데 이 앱은 openUrl()과 open()만 쓴다.",
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "opener:allow-default-urls",
    "dialog:allow-open"
  ]
}
